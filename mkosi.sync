#!/bin/bash
set -uexo pipefail

mkdir -p aur-pkg/
aur_packages=$(cat aur-packages)

cd aur-pkg/
for package in $aur_packages; do
    [[ ! -d $package ]] && git clone https://aur.archlinux.org/${package}.git || true
    pushd $package; git pull; popd
done
