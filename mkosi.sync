#!/bin/bash
set -uexo pipefail

mkdir -p aur-pkg/
cd aur-pkg/

for package in systemd-gadget dracut-sshd-git; do
    [[ ! -d $package ]] && git clone https://aur.archlinux.org/${package}.git || true
    pushd $package; git pull; popd
done
